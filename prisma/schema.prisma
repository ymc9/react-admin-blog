//////////////////////////////////////////////////////////////////////////////////////////////
// DO NOT MODIFY THIS FILE                                                                  //
// This file is automatically generated by ZenStack CLI and should not be manually updated. //
//////////////////////////////////////////////////////////////////////////////////////////////

datasource db {
    provider = "sqlite"
    url = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

/// @@allow('read', auth() != null)
/// @@allow('all', auth() == this || auth().role == 'Admin')
model User {
    id Int @id() @default(autoincrement())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt()
    email String @unique()
    /// @password
    /// @omit
    password String
    name String?
    role String @default("Author")
    posts Post[]
}

/// @@allow('all', auth().role == 'Admin')
/// @@allow('create', auth() == user && status == 'Draft')
/// @@allow('read', auth() == user)
/// @@allow('read', auth() != null && status != 'Draft')
/// @@allow('update', auth().role == 'Author' && auth() == user && status != 'Published' && future().status != 'Published')
/// @@allow('update', auth().role == 'Editor' && status != 'Draft')
/// @@allow('delete', auth() == user && status != 'Published')
model Post {
    id Int @id() @default(autoincrement())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt()
    title String
    content String
    status String @default("Draft")
    user User @relation(fields: [userId], references: [id], onDelete: Cascade)
    userId Int
}